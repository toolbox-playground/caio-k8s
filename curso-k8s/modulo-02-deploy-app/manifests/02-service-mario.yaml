apiVersion: v1
kind: Service
metadata:
  name: super-mario-service
  namespace: games
  labels:
    app: super-mario
spec:
  type: NodePort
  selector:
    app: super-mario
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30090
    protocol: TCP
    name: http
  sessionAffinity: None
