apiVersion: v1
kind: Service
metadata:
  name: super-mario-service
spec:
  type: ClusterIP
  selector:
    app: super-mario
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
