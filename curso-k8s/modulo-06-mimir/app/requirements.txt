# ── Web framework ────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3

# ── OpenTelemetry API + SDK (core) ───────────────────────────
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# ── Exportador OTLP via gRPC ─────────────────────────────────
opentelemetry-exporter-otlp-proto-grpc==1.29.0

# ── Instrumentação automática para FastAPI ───────────────────
opentelemetry-instrumentation-fastapi==0.50b0

# ── Semantic conventions ─────────────────────────────────────
opentelemetry-semantic-conventions==0.50b0

# ── Grafana Pyroscope — Continuous Profiling ─────────────────
# SDK Python para profiling contínuo.
# Implementação em Rust (substituiu o py-spy/C extension da 0.8.x),
# envia pprof profiles para o Pyroscope Server via HTTP a cada 15s.
#
# Overhead: <1% CPU, <5MB memória extra.
# Requer: Python 3.10+ (ABI estável cp310-abi3).
pyroscope-io==1.0.8

# ── Grafana Pyroscope — Span Profiles (Trace ↔ Profile bridge) ────────────────
# Registra o PyroscopeSpanProcessor no TracerProvider do OpenTelemetry.
# Adiciona o atributo pyroscope.profile.id em cada span, permitindo que o
# Grafana Tempo exiba o botão "Profiles for this span" com flame graph inline.
pyroscope-otel
